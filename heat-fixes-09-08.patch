diff --git a/packages/ui/src/components/CrateCalculator.astro b/packages/ui/src/components/CrateCalculator.astro
index 52585ad..43aff81 100644
--- a/packages/ui/src/components/CrateCalculator.astro
+++ b/packages/ui/src/components/CrateCalculator.astro
@@ -186,6 +186,7 @@ const L = {
 </section>
 
 <script>
+  import { attachCombobox, type ComboItem } from "../lib/combobox";
   const L = JSON.parse(document.getElementById("crx-labels")?.textContent ?? "{}");
   const S = L.s ?? {};
   const byId = (id: string) => document.getElementById(id);
@@ -252,32 +253,21 @@ const L = {
   byId("u-in")?.addEventListener("click", () => setUnit("in"));
   updateUnitLabels();
 
-  // --- Accent-insensitive autocomplete (same as the other tools) ---
-  function attachAC(input: HTMLInputElement) {
-    const dl = document.getElementById(input.getAttribute("data-ac") || "");
-    if (!dl) return;
-    const opts = [...dl.querySelectorAll("option")].map((o) => { const v = o.getAttribute("value") ?? ""; return { v, n: norm(v) }; });
-    const field = (input.closest(".crx__field") ?? input.parentElement) as HTMLElement | null;
-    if (!field) return;
-    field.classList.add("ac-host");
-    const box = document.createElement("ul"); box.className = "ac-list"; box.hidden = true; field.appendChild(box);
-    let cur = -1; let shown: { v: string; n: string }[] = [];
-    const close = () => { box.hidden = true; box.innerHTML = ""; cur = -1; };
-    const mark = () => { [...box.children].forEach((el, i) => (el as HTMLElement).classList.toggle("is-cur", i === cur)); };
-    const open = (q: string) => { const nq = norm(q); shown = (nq ? opts.filter((o) => o.n.includes(nq)) : opts).slice(0, 60);
-      if (!shown.length) { close(); return; } box.innerHTML = shown.map((o, i) => `<li class="ac-item" data-i="${i}">${esc(o.v)}</li>`).join(""); box.hidden = false; cur = -1; };
-    const choose = (i: number) => { const o = shown[i]; if (!o) return; input.value = o.v; close(); input.dispatchEvent(new Event("change", { bubbles: true })); };
-    input.addEventListener("input", () => open(input.value));
-    input.addEventListener("focus", () => open(input.value));
-    input.addEventListener("keydown", (e: KeyboardEvent) => { if (box.hidden) return;
-      if (e.key === "ArrowDown") { e.preventDefault(); cur = Math.min(cur + 1, shown.length - 1); mark(); }
-      else if (e.key === "ArrowUp") { e.preventDefault(); cur = Math.max(cur - 1, 0); mark(); }
-      else if (e.key === "Enter" && cur >= 0) { e.preventDefault(); choose(cur); }
-      else if (e.key === "Escape") close(); });
-    box.addEventListener("mousedown", (e: MouseEvent) => { const li = (e.target as HTMLElement).closest(".ac-item") as HTMLElement | null; if (li) { e.preventDefault(); choose(Number(li.dataset.i)); } });
-    document.addEventListener("click", (e) => { if (!field.contains(e.target as Node)) close(); });
-  }
-  document.querySelectorAll<HTMLInputElement>("[data-ac]").forEach((el) => attachAC(el));
+  // --- Accent-insensitive autocomplete, shared with the other tools.
+  // Retest 09/08/2026, point 9 : motif ARIA complet centralisé dans lib/combobox.ts. ---
+  const datalistItems = (listId: string) => {
+    const dl = document.getElementById(listId);
+    const opts = dl ? [...dl.querySelectorAll("option")].map((o) => o.getAttribute("value") ?? "") : [];
+    return (q: string): ComboItem[] => {
+      const nq = norm(q);
+      return (nq ? opts.filter((v) => norm(v).includes(nq)) : opts).slice(0, 60).map((v) => ({ display: v, value: v }));
+    };
+  };
+  document.querySelectorAll<HTMLInputElement>("[data-ac]").forEach((el) => {
+    const listId = el.getAttribute("data-ac");
+    if (!listId) return;
+    attachCombobox({ input: el, hostSelector: ".crx__field", getItems: datalistItems(listId) });
+  });
 
   // Race et compagnie pré-remplies depuis « ?breed= » / « ?air= » UNIQUEMENT, jamais depuis la
   // seule mémoire de session (voir le commentaire détaillé dans FlightFinder.astro) : une valeur
diff --git a/packages/ui/src/components/DestinationFinder.astro b/packages/ui/src/components/DestinationFinder.astro
index 6e1f31f..fd54e0c 100644
--- a/packages/ui/src/components/DestinationFinder.astro
+++ b/packages/ui/src/components/DestinationFinder.astro
@@ -183,6 +183,7 @@ const L = {
 </section>
 
 <script>
+  import { attachCombobox, type ComboItem } from "../lib/combobox";
   const L = JSON.parse(document.getElementById("dfx-labels")?.textContent ?? "{}");
   const S = L.s ?? {};
   const byId = (id: string) => document.getElementById(id);
@@ -233,42 +234,21 @@ const L = {
     return aid ? { ids: [aid], primary: aid } : { ids: [], primary: "" };
   };
 
-  // --- Custom accent-insensitive autocomplete (same behaviour as the flight finder) ---
-  function attachAC(input: HTMLInputElement) {
-    const listId = input.getAttribute("data-ac");
-    const dl = listId ? document.getElementById(listId) : null;
-    if (!dl) return;
-    const opts = [...dl.querySelectorAll("option")].map((o) => { const v = o.getAttribute("value") ?? ""; return { v, n: norm(v) }; });
-    const field = (input.closest(".dfx__field") ?? input.parentElement) as HTMLElement | null;
-    if (!field) return;
-    field.classList.add("ac-host");
-    const box = document.createElement("ul");
-    box.className = "ac-list"; box.setAttribute("role", "listbox"); box.hidden = true;
-    field.appendChild(box);
-    let cur = -1; let shown: { v: string; n: string }[] = [];
-    const close = () => { box.hidden = true; box.innerHTML = ""; cur = -1; input.setAttribute("aria-expanded", "false"); };
-    const mark = () => { [...box.children].forEach((el, i) => (el as HTMLElement).classList.toggle("is-cur", i === cur)); (box.children[cur] as HTMLElement | undefined)?.scrollIntoView({ block: "nearest" }); };
-    const openWith = (q: string) => {
+  // --- Custom accent-insensitive autocomplete, shared with the other tools.
+  // Retest 09/08/2026, point 9 : motif ARIA complet centralisé dans lib/combobox.ts. ---
+  const datalistItems = (listId: string) => {
+    const dl = document.getElementById(listId);
+    const opts = dl ? [...dl.querySelectorAll("option")].map((o) => o.getAttribute("value") ?? "") : [];
+    return (q: string): ComboItem[] => {
       const nq = norm(q);
-      shown = (nq ? opts.filter((o) => o.n.includes(nq)) : opts).slice(0, 60);
-      if (!shown.length) { close(); return; }
-      box.innerHTML = shown.map((o, i) => `<li class="ac-item" role="option" data-i="${i}">${esc(o.v)}</li>`).join("");
-      box.hidden = false; cur = -1; input.setAttribute("aria-expanded", "true");
+      return (nq ? opts.filter((v) => norm(v).includes(nq)) : opts).slice(0, 60).map((v) => ({ display: v, value: v }));
     };
-    const choose = (i: number) => { const o = shown[i]; if (!o) return; input.value = o.v; close(); input.dispatchEvent(new Event("change", { bubbles: true })); };
-    input.addEventListener("input", () => openWith(input.value));
-    input.addEventListener("focus", () => openWith(input.value));
-    input.addEventListener("keydown", (e: KeyboardEvent) => {
-      if (box.hidden) return;
-      if (e.key === "ArrowDown") { e.preventDefault(); cur = Math.min(cur + 1, shown.length - 1); mark(); }
-      else if (e.key === "ArrowUp") { e.preventDefault(); cur = Math.max(cur - 1, 0); mark(); }
-      else if (e.key === "Enter" && cur >= 0) { e.preventDefault(); choose(cur); }
-      else if (e.key === "Escape") { close(); }
-    });
-    box.addEventListener("mousedown", (e: MouseEvent) => { const li = (e.target as HTMLElement).closest(".ac-item") as HTMLElement | null; if (li) { e.preventDefault(); choose(Number(li.dataset.i)); } });
-    document.addEventListener("click", (e) => { if (!field.contains(e.target as Node)) close(); });
-  }
-  document.querySelectorAll<HTMLInputElement>("[data-ac]").forEach((el) => attachAC(el));
+  };
+  document.querySelectorAll<HTMLInputElement>("[data-ac]").forEach((el) => {
+    const listId = el.getAttribute("data-ac");
+    if (!listId) return;
+    attachCombobox({ input: el, hostSelector: ".dfx__field", getItems: datalistItems(listId) });
+  });
 
   // Race pré-remplie depuis « ?breed= » UNIQUEMENT, jamais depuis la seule mémoire de session :
   // une valeur mémorisée plus tôt n'est pas une intention, et un outil qui décide seul de quel
diff --git a/packages/ui/src/components/FlightFinder.astro b/packages/ui/src/components/FlightFinder.astro
index d8d34f5..5c57130 100644
--- a/packages/ui/src/components/FlightFinder.astro
+++ b/packages/ui/src/components/FlightFinder.astro
@@ -289,6 +289,7 @@ const labels = {
 <script>
   import type { DecisionReport, ReportItem, FinderRequest } from "@mydogcanfly/engine";
   import { API_BASE } from "../lib/env";
+  import { attachCombobox, type ComboItem } from "../lib/combobox";
 
   const form = document.getElementById("mdcf-finder") as HTMLFormElement | null;
   const out = document.getElementById("mdcf-finder-result") as HTMLDivElement | null;
@@ -598,44 +599,23 @@ const labels = {
 
   // --- Custom autocomplete: replaces the native <datalist>, whose filtering is accent-SENSITIVE
   // (typing "grece" would drop "Grèce" and surface "Zagreb"). This one matches on the accent-folded
-  // form, so "gre"/"grece" lists every Greek airport, "perou" finds Lima, etc. ---
-  function attachAC(input: HTMLInputElement) {
-    const listId = input.getAttribute("data-ac");
-    const dl = listId ? document.getElementById(listId) : null;
-    if (!dl) return;
-    const opts = [...dl.querySelectorAll("option")].map((o) => { const v = o.getAttribute("value") ?? ""; return { v, n: norm(v) }; });
-    const field = (input.closest(".finder__field") ?? input.parentElement) as HTMLElement | null;
-    if (!field) return;
-    field.classList.add("ac-host");
-    const box = document.createElement("ul");
-    box.className = "ac-list"; box.setAttribute("role", "listbox"); box.hidden = true;
-    field.appendChild(box);
-    let cur = -1;
-    let shown: { v: string; n: string }[] = [];
-    const esc2 = (s: string) => s.replace(/[&<>]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;" }[c] ?? c));
-    const close = () => { box.hidden = true; box.innerHTML = ""; cur = -1; input.setAttribute("aria-expanded", "false"); };
-    const mark = () => { [...box.children].forEach((el, i) => (el as HTMLElement).classList.toggle("is-cur", i === cur)); (box.children[cur] as HTMLElement | undefined)?.scrollIntoView({ block: "nearest" }); };
-    const openWith = (q: string) => {
+  // form, so "gre"/"grece" lists every Greek airport, "perou" finds Lima, etc.
+  // Retest 09/08/2026, point 9 : implémentation ARIA propre centralisée dans lib/combobox.ts,
+  // partagée par les 4 outils (Finder, Destinations, Cage, Chaleur) — voir ce fichier pour le détail
+  // du motif combobox/listbox/option + aria-controls/aria-activedescendant. ---
+  const datalistItems = (listId: string) => {
+    const dl = document.getElementById(listId);
+    const opts = dl ? [...dl.querySelectorAll("option")].map((o) => o.getAttribute("value") ?? "") : [];
+    return (q: string): ComboItem[] => {
       const nq = norm(q);
-      shown = (nq ? opts.filter((o) => o.n.includes(nq)) : opts).slice(0, 60);
-      if (!shown.length) { close(); return; }
-      box.innerHTML = shown.map((o, i) => `<li class="ac-item" role="option" data-i="${i}">${esc2(o.v)}</li>`).join("");
-      box.hidden = false; cur = -1; input.setAttribute("aria-expanded", "true");
+      return (nq ? opts.filter((v) => norm(v).includes(nq)) : opts).slice(0, 60).map((v) => ({ display: v, value: v }));
     };
-    const choose = (i: number) => { const o = shown[i]; if (!o) return; input.value = o.v; close(); input.dispatchEvent(new Event("change", { bubbles: true })); };
-    input.addEventListener("input", () => openWith(input.value));
-    input.addEventListener("focus", () => openWith(input.value));
-    input.addEventListener("keydown", (e: KeyboardEvent) => {
-      if (box.hidden) return;
-      if (e.key === "ArrowDown") { e.preventDefault(); cur = Math.min(cur + 1, shown.length - 1); mark(); }
-      else if (e.key === "ArrowUp") { e.preventDefault(); cur = Math.max(cur - 1, 0); mark(); }
-      else if (e.key === "Enter" && cur >= 0) { e.preventDefault(); choose(cur); }
-      else if (e.key === "Escape") { close(); }
-    });
-    box.addEventListener("mousedown", (e: MouseEvent) => { const li = (e.target as HTMLElement).closest(".ac-item") as HTMLElement | null; if (li) { e.preventDefault(); choose(Number(li.dataset.i)); } });
-    document.addEventListener("click", (e) => { if (!field.contains(e.target as Node)) close(); });
-  }
-  document.querySelectorAll<HTMLInputElement>("[data-ac]").forEach((el) => attachAC(el));
+  };
+  document.querySelectorAll<HTMLInputElement>("[data-ac]").forEach((el) => {
+    const listId = el.getAttribute("data-ac");
+    if (!listId) return;
+    attachCombobox({ input: el, hostSelector: ".finder__field", getItems: datalistItems(listId) });
+  });
 
   const ctx = (window as { mdcfCtx?: { breed?: string; setBreed?: (v: string) => void } }).mdcfCtx;
   /**
@@ -676,9 +656,45 @@ const labels = {
     } catch (e) { return false; }
   })();
 
+  /* Retest 09/08/2026, point 6 (rapport HeatCalculator) : le lien « Trouver la meilleure compagnie
+   * pour ce voyage » ne transmettait que la race, pas le trajet. `to=<iso2>` existe déjà mais
+   * désigne un PAYS (fiches pays) — impossible à réutiliser tel quel pour un AÉROPORT précis sans
+   * ambiguïté. Deux nouveaux paramètres dédiés, `from`/`dest` (identifiants aéroport internes,
+   * même convention que `breed=breed_xxx`), coexistent avec `to` sans le redéfinir. `month` prend
+   * une date indicative (1er jour du mois demandé, année en cours ou suivante si le mois est déjà
+   * passé) — HeatCalculator ne connaît qu'un mois, pas un jour précis. */
+  let originFromDeepLink = false;
+  (function () {
+    try {
+      const q = window.mdcfQuery();
+      const originEl = byId("f-origin") as HTMLInputElement | null;
+      const fromId = q.get("from");
+      if (fromId && originEl) {
+        const label = (L.airCityLabel?.[fromId] as string | undefined) ?? (L.airRev?.[fromId] as string | undefined);
+        if (label) { originEl.value = label; originFromDeepLink = true; }
+      }
+      const destId = q.get("dest");
+      const destEl = byId("f-dest") as HTMLInputElement | null;
+      if (destId && destEl && !prefilledDest) {
+        const label = (L.airCityLabel?.[destId] as string | undefined) ?? (L.airRev?.[destId] as string | undefined);
+        if (label) destEl.value = label;
+      }
+      const month = parseInt(q.get("month") || "", 10);
+      const dateEl = byId("f-date") as HTMLInputElement | null;
+      if (month >= 1 && month <= 12 && dateEl && !dateEl.value) {
+        const now = new Date();
+        let year = now.getFullYear();
+        if (month < now.getMonth() + 1) year += 1; // mois déjà passé cette année → indicatif l'an prochain
+        dateEl.value = `${year}-${String(month).padStart(2, "0")}-01`;
+      }
+    } catch (e) { /* ignore */ }
+  })();
+
   // Prefill "From" with the nearest airport, using Cloudflare's edge geolocation (no browser permission prompt).
+  // Une intention explicite (?from=<id> ci-dessus) l'emporte toujours sur une déduction géographique.
   (async () => {
     try {
+      if (originFromDeepLink) return;
       const r = await fetch(`${API_BASE}/v1/nearest-airport`);
       if (!r.ok) return;
       const { airport_id, unit } = (await r.json()) as { airport_id?: string; unit?: "kg" | "lb" };
diff --git a/packages/ui/src/components/HeatCalculator.astro b/packages/ui/src/components/HeatCalculator.astro
index 39757bc..a40883f 100644
--- a/packages/ui/src/components/HeatCalculator.astro
+++ b/packages/ui/src/components/HeatCalculator.astro
@@ -49,22 +49,41 @@ const L = {
     submit: T("Check the heat risk", "Évaluer le risque chaleur", "Evaluar el riesgo por calor"),
     needBoth: T("Pick a departure and an arrival airport.", "Choisis un aéroport de départ et un d'arrivée.", "Elige un aeropuerto de salida y uno de llegada."),
     at: T("at", "à", "a"),
-    estimate: T("Seasonal estimate", "Estimation saisonnière", "Estimación estacional"),
+    // Retest 09/08/2026, point 4 (option A) : le libellé affirmait une "estimation saisonnière"
+    // sans réserve suffisante pour un usage aussi sensible qu'un embargo chaleur — renommé pour
+    // dire clairement le niveau de précision réel (latitude + mois, sans altitude ni climat local).
+    estimate: T("Very approximate climate indicator", "Indicateur climatique très approximatif", "Indicador climático muy aproximado"),
     verdictTitle: T("Hold / cargo heat verdict", "Verdict chaleur soute / cargo", "Veredicto de calor en bodega / carga"),
     ok: T("Clear to fly", "Feu vert", "Luz verde para volar"),
     risk: T("Elevated risk", "Risque élevé", "Riesgo elevado"),
-    embargo: T("Likely embargo", "Embargo probable", "Embargo probable"),
+    // Retest 09/08/2026, point 5 : "Embargo probable" affirmait un pronostic sur LE vol réservé ;
+    // la valeur sous-jacente est une moyenne climatique mensuelle, pas la météo du jour du vol.
+    embargo: T("Historically high embargo-risk period", "Période historiquement à fort risque d'embargo", "Periodo históricamente de alto riesgo de embargo"),
     okD: T("Estimated temperatures are within the safe range for hold/cargo transport.", "Les températures estimées sont dans la plage sûre pour un transport en soute/cargo.", "Las temperaturas estimadas están dentro del rango seguro para el transporte en bodega/carga."),
     riskD: T("Warm enough that airlines may restrict hold/cargo travel — confirm with the carrier.", "Assez chaud pour que des compagnies restreignent la soute/cargo — à confirmer avec la compagnie.", "Hace suficiente calor como para que las aerolíneas restrinjan la bodega/carga: confírmalo con la aerolínea."),
-    embargoD: T("Above the usual embargo threshold — hold/cargo is likely suspended on this route/month.", "Au-dessus du seuil d'embargo habituel — la soute/cargo est probablement suspendue sur cette route/ce mois.", "Por encima del umbral de embargo habitual: es probable que la bodega/carga esté suspendida en esta ruta/mes."),
+    embargoD: T("Above the usual embargo threshold — hold/cargo has historically often been suspended on this route/month.", "Au-dessus du seuil d'embargo habituel — la soute/cargo a historiquement souvent été suspendue sur cette route/ce mois.", "Por encima del umbral de embargo habitual: la bodega/carga ha estado históricamente suspendida a menudo en esta ruta/mes."),
+    // Retest 09/08/2026, point 5 : précise que la valeur est une moyenne climatique mensuelle, pas
+    // la température maximale ni celle réellement prévue le jour du vol.
+    avgClarify: T("This value is a monthly climate average — not the maximum temperature, nor the forecast for the actual flight day.", "Cette valeur est une moyenne climatique mensuelle, pas la température maximale ni celle du jour du vol.", "Este valor es un promedio climático mensual, no la temperatura máxima ni la prevista para el día real del vuelo."),
+    // Retest 09/08/2026, point 7 : les seuils (déjà codés) n'étaient jamais montrés à l'utilisateur.
+    thresholdLine: T("MyDogCanFly indicative threshold: amber from {risk} °C, red from {emb} °C.", "Seuil indicatif MyDogCanFly : ambre à partir de {risk} °C, rouge à partir de {emb} °C.", "Umbral indicativo de MyDogCanFly: ámbar a partir de {risk} °C, rojo a partir de {emb} °C."),
+    thresholdCaveat: T("Some carriers apply a lower threshold, or an independent seasonal embargo regardless of temperature.", "Certaines compagnies appliquent un seuil plus bas, ou un embargo saisonnier indépendant de la température.", "Algunas aerolíneas aplican un umbral más bajo, o un embargo estacional independiente de la temperatura."),
     brachy: T("Snub-nosed breed: stricter limits apply, and many carriers ban them from the hold year-round regardless of temperature.", "Race brachycéphale : seuils plus stricts, et de nombreuses compagnies l'interdisent en soute toute l'année, quelle que soit la température.", "Raza de hocico chato: se aplican límites más estrictos y muchas aerolíneas las prohíben en bodega todo el año, sea cual sea la temperatura."),
     calendar: T("Heat by month on this route", "Chaleur par mois sur cette route", "Calor por mes en esta ruta"),
-    calNote: T("Worst of the two airports each month. Green = clear, amber = risk, red = likely embargo.", "Pire des deux aéroports chaque mois. Vert = ok, ambre = risque, rouge = embargo probable.", "El peor de los dos aeropuertos cada mes. Verde = ok, ámbar = riesgo, rojo = embargo probable."),
+    calNote: T("Worst of the two airports each month. Green = clear, amber = risk, red = historically high embargo risk.", "Pire des deux aéroports chaque mois. Vert = ok, ambre = risque, rouge = période à risque d'embargo élevé.", "El peor de los dos aeropuertos cada mes. Verde = ok, ámbar = riesgo, rojo = periodo de alto riesgo de embargo."),
     bestMonths: T("Best months", "Meilleurs mois", "Mejores meses"),
-    caveat: T("Estimates from airport latitude + month — not a live forecast. Heat embargoes apply to hold/cargo; a small dog travelling in the climate-controlled cabin is not affected. Always confirm with the airline.", "Estimations d'après la latitude de l'aéroport + le mois — pas une prévision en temps réel. Les embargos chaleur concernent la soute/cargo ; un petit chien voyageant en cabine climatisée n'est pas concerné. Confirme toujours avec la compagnie.", "Estimaciones a partir de la latitud del aeropuerto + el mes, no una previsión en tiempo real. Los embargos por calor afectan a la bodega/carga; un perro pequeño que viaja en la cabina climatizada no se ve afectado. Confirma siempre con la aerolínea."),
+    // Retest 09/08/2026, point 8 : pour une race brachycéphale, "Meilleurs mois" laissait entendre
+    // qu'elle serait acceptée en soute ces mois-là, alors que beaucoup de compagnies l'interdisent
+    // toute l'année indépendamment de la température (cf. la note "brachy" déjà affichée juste au-dessus).
+    bestMonthsBrachy: T("Climatically least unfavourable months — this does not mean the breed is accepted in the hold.", "Mois climatiquement les moins défavorables — cela ne signifie pas que la race est acceptée en soute.", "Meses climáticamente menos desfavorables: esto no significa que la raza esté aceptada en bodega."),
+    caveat: T("Estimates from airport latitude + month only — no altitude, coastal/continental climate, or local normals. Not a live forecast. Heat embargoes apply to hold/cargo; a small dog travelling in the climate-controlled cabin is not affected. Always confirm with the airline.", "Estimations d'après la seule latitude de l'aéroport + le mois — sans altitude, climat continental/maritime ni normales locales. Pas une prévision en temps réel. Les embargos chaleur concernent la soute/cargo ; un petit chien voyageant en cabine climatisée n'est pas concerné. Confirme toujours avec la compagnie.", "Estimaciones basadas solo en la latitud del aeropuerto + el mes, sin altitud, clima continental/marítimo ni normales locales. No es una previsión en tiempo real. Los embargos por calor afectan a la bodega/carga; un perro pequeño que viaja en la cabina climatizada no se ve afectado. Confirma siempre con la aerolínea."),
     onwardAir: T("Find the best airline for this trip", "Trouver la meilleure compagnie pour ce voyage", "Encontrar la mejor aerolínea para este viaje"),
     onwardCrate: T("Which travel crate size?", "Quelle taille de cage ?", "¿Qué talla de jaula?"),
     degC: "°C",
+    // Retest 09/08/2026, points 1, 2, 3
+    breedInvalid: T("Choose a breed from the list, or clear the field to continue without one.", "Sélectionne une race dans la liste ou efface le champ pour continuer sans race.", "Elige una raza de la lista, o borra el campo para continuar sin raza."),
+    staleHint: T("Criteria changed — rerun the check.", "Critères modifiés — relance l'évaluation.", "Criterios modificados: repite la evaluación."),
+    sameAirport: T("Departure and arrival must be different.", "Le départ et l'arrivée doivent être différents.", "La salida y la llegada deben ser diferentes."),
   },
 };
 ---
@@ -85,6 +104,7 @@ const L = {
 
 <script type="application/json" id="hx-data" set:html={JSON.stringify(L)} is:inline />
 <script>
+  import { attachCombobox } from "../lib/combobox";
   const L = JSON.parse(document.getElementById("hx-data").textContent);
   const $ = (s) => document.querySelector(s);
   const norm = (s) => (s ?? "").normalize("NFD").replace(/[̀-ͯ]/g, "").toLowerCase().trim();
@@ -113,40 +133,57 @@ const L = {
   }
   const label = (a)=> a.city+" ("+a.iata+")";
 
-  // --- autocomplete ---
+  // --- autocomplete, shared with the other tools.
+  // Retest 09/08/2026, point 9 : motif ARIA complet centralisé dans lib/combobox.ts — cet outil
+  // n'avait auparavant aucun rôle ARIA sur sa liste de suggestions. ---
   const breedLabels=Object.values(L.breedById);
   function acItems(key, q){
     const n=norm(q);
-    if(key==="breed"){ return (n?breedLabels.filter(k=>norm(k).includes(n)):breedLabels).slice(0,8).map(k=>({v:k})); }
+    if(key==="breed"){ return (n?breedLabels.filter(k=>norm(k).includes(n)):breedLabels).slice(0,8).map(k=>({display:k,value:k})); }
     const src=L.airports;
     const arr=(n?src.filter(a=>norm(a.city+" "+a.iata+" "+a.country).includes(n)):src).slice(0,8);
-    return arr.map(a=>({v:label(a)+" — "+a.country}));
+    return arr.map(a=>({display:label(a)+" — "+a.country, value:label(a)}));
   }
-  function attachAC(input){
-    const key=input.getAttribute("data-ac"); const field=input.parentElement; field.style.position="relative";
-    const box=document.createElement("ul"); box.className="ac-list"; box.hidden=true; field.appendChild(box); let cur=-1, shown=[];
-    const close=()=>{ box.hidden=true; input.setAttribute("aria-expanded","false"); };
-    const render=()=>{ shown=acItems(key, input.value); if(!shown.length){close();return;} box.innerHTML=shown.map((o,i)=>`<li class="ac-item" data-i="${i}">${esc(o.v)}</li>`).join(""); box.hidden=false; input.setAttribute("aria-expanded","true"); cur=-1; };
-    const choose=(i)=>{ const o=shown[i]; if(!o)return; input.value=o.v.split(" — ")[0]; close(); };
-    input.addEventListener("input",render);
-    input.addEventListener("focus",render);
-    input.addEventListener("keydown",(e)=>{ if(box.hidden)return; if(e.key==="ArrowDown"){e.preventDefault();cur=Math.min(cur+1,shown.length-1);}else if(e.key==="ArrowUp"){e.preventDefault();cur=Math.max(cur-1,0);}else if(e.key==="Enter"){if(cur>=0){e.preventDefault();choose(cur);}return;}else if(e.key==="Escape"){close();return;}else return; [...box.children].forEach((li,i)=>li.classList.toggle("is-cur",i===cur)); });
-    box.addEventListener("mousedown",(e)=>{ const li=e.target.closest(".ac-item"); if(li){e.preventDefault();choose(Number(li.dataset.i));} });
-    document.addEventListener("click",(e)=>{ if(!field.contains(e.target))close(); });
-  }
-  ["#hx-dep","#hx-arr","#hx-breed"].forEach((s)=>attachAC($(s)));
+  ["#hx-dep","#hx-arr","#hx-breed"].forEach((s)=>{
+    const input=$(s);
+    const key=input.getAttribute("data-ac");
+    attachCombobox({ input, hostSelector: ".hx-field", getItems: (q)=>acItems(key, q) });
+  });
 
   const badge=(lv,txt)=>`<span class="hx-pill hx-${lv}">${txt}</span>`;
   const emoji={ok:"🟢",warn:"🟡",no:"🔴"};
 
+  // Retest 09/08/2026, point 2 : aucun mécanisme n'invalidait un résultat affiché quand un critère
+  // changeait ensuite (départ, arrivée, mois, race) — même patron que les 3 autres outils.
+  function markStale(){
+    const out=$("#hx-out");
+    if(!out||out.hidden) return;
+    if(out.querySelector(".hx-msg")) return; // pas un vrai résultat (message d'aide/erreur)
+    if(out.querySelector(".hx-stale")) return;
+    const banner=document.createElement("div");
+    banner.className="hx-stale";
+    banner.textContent=L.s.staleHint;
+    out.insertBefore(banner, out.firstChild);
+  }
+  ["#hx-dep","#hx-arr","#hx-breed"].forEach((s)=>{ const el=$(s); el?.addEventListener("input",markStale); el?.addEventListener("change",markStale); });
+  $("#hx-month")?.addEventListener("change",markStale);
+
   $("#hx-form").addEventListener("submit",(e)=>{
     e.preventDefault();
     const dep=findAirport($("#hx-dep").value), arr=findAirport($("#hx-arr").value);
     const out=$("#hx-out"); out.hidden=false;
     if(!dep||!arr){ out.innerHTML=`<div class="hx-msg">${L.s.needBoth}</div>`; return; }
-    const month=parseInt($("#hx-month").value,10);
-    const bv=norm($("#hx-breed").value);
+    // Retest 09/08/2026, point 3 : même aéroport accepté comme départ ET arrivée.
+    if(dep.id===arr.id){ out.innerHTML=`<div class="hx-msg">${L.s.sameAirport}</div>`; return; }
+    // Retest 09/08/2026, point 1 : une race tapée sans correspondance retombait silencieusement sur
+    // "sans race" (donc sans seuils brachycéphales) alors que le champ affichait toujours le texte
+    // saisi — texte et race sélectionnée sont désormais distingués, et une saisie non reconnue
+    // bloque le calcul plutôt que de l'ignorer.
+    const breedRaw=$("#hx-breed").value.trim();
+    const bv=norm(breedRaw);
     const bkey=bv?Object.keys(L.breeds).find(k=>norm(k)===bv):null;
+    if(breedRaw && !bkey){ out.innerHTML=`<div class="hx-msg">${L.s.breedInvalid}</div>`; return; }
+    const month=parseInt($("#hx-month").value,10);
     const breed=bkey?L.breeds[bkey]:null;
     const brachy=!!(breed&&breed.br); const b=bands(brachy);
 
@@ -160,26 +197,56 @@ const L = {
     const cal=[]; for(let m=1;m<=12;m++){ const w=Math.max(tempAt(dep.lat,m),tempAt(arr.lat,m)); cal.push({m,t:w,lv:level(w,b)}); }
     const best=cal.filter(c=>c.lv==="ok").map(c=>L.months[c.m-1]);
 
+    // Retest 09/08/2026, point 7 : les seuils utilisés par le calcul (déjà codés) n'étaient jamais
+    // montrés à l'utilisateur.
+    const threshLine=L.s.thresholdLine.replace("{risk}",b.risk).replace("{emb}",b.emb);
+
+    // Retest 09/08/2026, point 6 : le lien vers le finder ne transmettait que la race. Il transmet
+    // maintenant aussi le trajet et le mois — `from`/`dest` (identifiants aéroport internes, même
+    // convention que `breed=breed_xxx`) + `month`, ajoutés dans le dièse via `mdcfPut` comme `breed`
+    // (voir le commentaire détaillé dans FlightFinder.astro : les paramètres restent volontairement
+    // après le dièse pour des raisons SEO, `to=<iso2>` désigne déjà un PAYS et n'est donc pas
+    // réutilisable tel quel pour un aéroport précis).
+    let finderHref=L.finderHref;
+    if (typeof window.mdcfPut === "function") {
+      finderHref = window.mdcfPut(finderHref, "from", dep.id);
+      finderHref = window.mdcfPut(finderHref, "dest", arr.id);
+      finderHref = window.mdcfPut(finderHref, "month", String(month));
+      if (breed) finderHref = window.mdcfPut(finderHref, "breed", breed.id);
+    } else if (breed) {
+      finderHref = `${finderHref}?breed=${breed.id}`; // repli si mdcfPut n'est pas chargé
+    }
+
+    // Retest 09/08/2026, point 8 : pour une race brachycéphale, "Meilleurs mois" laissait entendre
+    // qu'elle serait acceptée en soute ces mois-là.
+    const bestBlock = best.length
+      ? (brachy
+          ? `<p class="hx-best">${esc(L.s.bestMonthsBrachy)} <span class="hx-best-list">${best.join(", ")}</span></p>`
+          : `<p class="hx-best"><b>${esc(L.s.bestMonths)}:</b> ${best.join(", ")}</p>`)
+      : "";
+
     out.innerHTML=`
       <div class="hx-card hx-verdict hx-b-${lv}">
         <div class="hx-vhead"><span class="hx-vemoji">${emoji[lv]}</span><span class="hx-vtitle">${vTitle}</span></div>
         <p class="hx-vdesc">${vDesc}</p>
+        <p class="hx-avgnote">${L.s.avgClarify}</p>
         <div class="hx-legs">
           <div class="hx-leg"><span class="hx-legcap">${esc(dep.city)} (${dep.iata})</span>${badge(level(dt,b),dt+L.s.degC)}</div>
           <div class="hx-legarrow">→</div>
           <div class="hx-leg"><span class="hx-legcap">${esc(arr.city)} (${arr.iata})</span>${badge(level(at,b),at+L.s.degC)}</div>
         </div>
         <p class="hx-est">${L.s.estimate} · ${L.months[month-1]}</p>
+        <p class="hx-thresh">${threshLine}<br>${L.s.thresholdCaveat}</p>
         ${brachy?`<p class="hx-brachy">⚠ ${L.s.brachy}</p>`:""}
       </div>
       <div class="hx-card">
         <div class="hx-calhd">${L.s.calendar}</div>
         <div class="hx-cal">${cal.map(c=>`<div class="hx-mon hx-m-${c.lv} ${c.m===month?"is-sel":""}"><span class="hx-mlab">${L.months[c.m-1]}</span><span class="hx-mt">${c.t}°</span></div>`).join("")}</div>
         <p class="hx-calnote">${L.s.calNote}</p>
-        ${best.length?`<p class="hx-best"><b>${L.s.bestMonths}:</b> ${best.join(", ")}</p>`:""}
+        ${bestBlock}
       </div>
       <div class="hx-onward">
-        <a class="hx-onav" href="${L.finderHref}${breed?`?breed=${breed.id}`:""}" data-carry="breed air">✈ ${L.s.onwardAir}</a>
+        <a class="hx-onav" href="${esc(finderHref)}" data-carry="breed air">✈ ${L.s.onwardAir}</a>
         <a class="hx-onav" href="${L.crateHref}" data-carry="breed air">📦 ${L.s.onwardCrate}</a>
       </div>
       <p class="hx-caveat">${L.s.caveat}</p>`;
@@ -231,6 +298,11 @@ const L = {
 
   .hx-out { margin-top: 20px; display: grid; gap: 14px; }
   :global(.hx-msg) { color: var(--mdcf-accent-ink); background: var(--mdcf-accent-soft); border-radius: 10px; padding: 12px 14px; font-size: 14px; }
+  /* Retest 09/08/2026, point 2 — bannière "critères modifiés", même palette que dans les 3 autres outils. */
+  :global(.hx-stale) { background: #fdf3df; border: 1px solid #f0e2be; color: #9a6400; border-radius: 10px; padding: 8px 12px; font-size: 13px; font-weight: 700; }
+  /* Retest 09/08/2026, points 5 et 7. */
+  :global(.hx-avgnote) { font-size: 12px; color: var(--mdcf-muted); margin: -8px 0 12px; font-style: italic; }
+  :global(.hx-thresh) { font-size: 12px; color: var(--mdcf-muted); margin: 10px 0 0; line-height: 1.5; }
   :global(.hx-card) { border: 1px solid var(--mdcf-line); border-radius: 14px; padding: 18px 20px; background: var(--mdcf-surface); }
   :global(.hx-verdict.hx-b-ok) { background: #f0faf3; border-color: #cfead8; }
   :global(.hx-verdict.hx-b-warn) { background: #fdf6e7; border-color: #f0e2be; }
diff --git a/packages/ui/src/lib/combobox.ts b/packages/ui/src/lib/combobox.ts
new file mode 100644
index 0000000..28ba430
--- /dev/null
+++ b/packages/ui/src/lib/combobox.ts
@@ -0,0 +1,116 @@
+/* Shared accent-insensitive autocomplete combobox, used by every onboard tool (Finder, Destinations,
+ * Crate, Heat). Retest 09/08/2026, point 9 : chaque outil dupliquait sa propre version, dont certaines
+ * (HeatCalculator) sans le moindre attribut ARIA (`role="listbox"`/`role="option"` absents) et TOUTES
+ * sans `aria-controls`/`aria-activedescendant` — un lecteur d'écran ou une navigation clavier ne pouvait
+ * pas savoir quelle option de la liste était actuellement mise en avant. Centralisé ici pour que le
+ * correctif (et les suivants) s'applique une seule fois, au lieu de re-diverger outil par outil.
+ *
+ * Implements the WAI-ARIA 1.2 "combobox with listbox popup" pattern: the input keeps
+ * role="combobox" + aria-autocomplete="list" (set in the markup), this module manages
+ * aria-expanded, aria-controls (pointing at the generated listbox id) and aria-activedescendant
+ * (pointing at the currently-highlighted option id) as the user types or moves with the arrow keys.
+ */
+
+export interface ComboItem {
+  /** Text shown in the dropdown (may carry extra context, e.g. "Paris (CDG) — France"). */
+  display: string;
+  /** Value written into the input once chosen (may be shorter than `display`). */
+  value: string;
+}
+
+export interface AttachComboboxOptions {
+  input: HTMLInputElement;
+  /** Returns the (already filtered/sliced) items to show for the current query. Empty query → full list. */
+  getItems: (query: string) => ComboItem[];
+  /** CSS selector for the positioning host (must be a `position: relative` ancestor). Falls back to input.parentElement. */
+  hostSelector?: string;
+  /** Called after the input's value is set and (unless disabled) a "change" event is dispatched. */
+  onChoose?: (value: string) => void;
+  /** Dispatch a bubbling "change" event on the input after a choice — default true (existing behaviour). */
+  emitChange?: boolean;
+}
+
+const escHtml = (s: string): string =>
+  s.replace(/[&<>"]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[c] ?? c));
+
+export function attachCombobox(o: AttachComboboxOptions): void {
+  const { input } = o;
+  const emitChange = o.emitChange !== false;
+  const field = (o.hostSelector ? input.closest(o.hostSelector) : null) as HTMLElement | null ?? input.parentElement;
+  if (!field) return;
+  field.classList.add("ac-host");
+
+  const listId = `${input.id || "ac"}-listbox`;
+  const optId = (i: number) => `${listId}-opt-${i}`;
+
+  const box = document.createElement("ul");
+  box.className = "ac-list";
+  box.id = listId;
+  box.setAttribute("role", "listbox");
+  box.hidden = true;
+  field.appendChild(box);
+  input.setAttribute("aria-controls", listId);
+
+  let cur = -1;
+  let shown: ComboItem[] = [];
+
+  const close = () => {
+    box.hidden = true;
+    box.innerHTML = "";
+    cur = -1;
+    input.setAttribute("aria-expanded", "false");
+    input.removeAttribute("aria-activedescendant");
+  };
+
+  const mark = () => {
+    [...box.children].forEach((el, i) => {
+      const on = i === cur;
+      el.classList.toggle("is-cur", on);
+      el.setAttribute("aria-selected", on ? "true" : "false");
+    });
+    if (cur >= 0) {
+      input.setAttribute("aria-activedescendant", optId(cur));
+      (box.children[cur] as HTMLElement | undefined)?.scrollIntoView({ block: "nearest" });
+    } else {
+      input.removeAttribute("aria-activedescendant");
+    }
+  };
+
+  const openWith = (q: string) => {
+    shown = o.getItems(q);
+    if (!shown.length) { close(); return; }
+    box.innerHTML = shown
+      .map((it, i) => `<li class="ac-item" role="option" id="${optId(i)}" aria-selected="false">${escHtml(it.display)}</li>`)
+      .join("");
+    box.hidden = false;
+    cur = -1;
+    input.setAttribute("aria-expanded", "true");
+    input.removeAttribute("aria-activedescendant");
+  };
+
+  const choose = (i: number) => {
+    const it = shown[i];
+    if (!it) return;
+    input.value = it.value;
+    close();
+    if (emitChange) input.dispatchEvent(new Event("change", { bubbles: true }));
+    o.onChoose?.(it.value);
+  };
+
+  input.addEventListener("input", () => openWith(input.value));
+  input.addEventListener("focus", () => openWith(input.value));
+  input.addEventListener("keydown", (e: KeyboardEvent) => {
+    if (box.hidden) return;
+    if (e.key === "ArrowDown") { e.preventDefault(); cur = Math.min(cur + 1, shown.length - 1); mark(); }
+    else if (e.key === "ArrowUp") { e.preventDefault(); cur = Math.max(cur - 1, 0); mark(); }
+    else if (e.key === "Enter" && cur >= 0) { e.preventDefault(); choose(cur); }
+    else if (e.key === "Escape") { close(); }
+  });
+  box.addEventListener("mousedown", (e: MouseEvent) => {
+    const li = (e.target as HTMLElement).closest(".ac-item") as HTMLElement | null;
+    if (!li) return;
+    const idx = [...box.children].indexOf(li);
+    if (idx >= 0) { e.preventDefault(); choose(idx); }
+  });
+  document.addEventListener("click", (e) => { if (!field!.contains(e.target as Node)) close(); });
+}
