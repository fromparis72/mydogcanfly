diff --git a/packages/ui/src/components/EntityIndex.astro b/packages/ui/src/components/EntityIndex.astro
index c6a87ee..3134c69 100644
--- a/packages/ui/src/components/EntityIndex.astro
+++ b/packages/ui/src/components/EntityIndex.astro
@@ -2,11 +2,25 @@
 import Base from "../layouts/Base.astro";
 import { t } from "@mydogcanfly/knowledge";
 interface Item { href: string; label: string; sub?: string; flag?: string }
-interface Props { kind: string; title: string; /** Titre SEO « mots clés d'abord » ; à défaut, H1 + suffixe de marque (ancien comportement). */ seoTitle?: string; blurb?: string; metaDesc?: string; items: Item[]; locale?: string; link?: { href: string; label: string } }
-const { kind, title, seoTitle, blurb, metaDesc, items, locale = "en", link } = Astro.props;
+interface Props { kind: string; title: string; /** Titre SEO « mots clés d'abord » ; à défaut, H1 + suffixe de marque (ancien comportement). */ seoTitle?: string; blurb?: string; metaDesc?: string; items: Item[]; locale?: string; link?: { href: string; label: string };
+  /** Emblème d'accueil réduit, haut droite (décision Philippe 10/08/2026) — opt-in explicite,
+   * volontairement PAS activé par défaut : ce composant est aussi utilisé par les pages aéroports
+   * (`airports/index.astro`, `airports/pet-relief.astro`), hors du périmètre demandé (seulement
+   * compagnies/destinations/races/outils, 4 pages précises, pas de sous-pages). */
+  showEmblem?: boolean }
+const { kind, title, seoTitle, blurb, metaDesc, items, locale = "en", link, showEmblem = false } = Astro.props;
 ---
 <Base title={seoTitle ?? `${title} — MyDogCanFly`} description={metaDesc ?? blurb ?? title} locale={locale}>
-  <section class="mdcf-section">
+  <section class={`mdcf-section${showEmblem ? " idx__hero" : ""}`}>
+    {showEmblem && (
+      /* Même emblème que la page d'accueil (packages/ui/public/brand/emblem.webp), repris ici en
+         plus petit (54 % de la taille d'accueil : 60 % puis -10 % relatif, décision Philippe
+         10/08/2026) en haut à droite. `alt` porte le titre déjà traduit de la page plutôt qu'un
+         texte vide : demandé explicitement pour l'indexation image (Google Images), au prix d'une
+         redite mineure avec le <h1> pour les lecteurs d'écran — arbitrage assumé, à la différence
+         de l'emblème d'accueil qui reste `alt=""`/`aria-hidden`. */
+      <img class="idx__emblem" src="/brand/emblem.webp" alt={title} width="440" height="252" decoding="async" loading="lazy" />
+    )}
     <div class="mdcf-container">
       <p class="mdcf-eyebrow">{kind}</p>
       <h1 class="mdcf-h2">{title}</h1>
@@ -74,6 +88,18 @@ const { kind, title, seoTitle, blurb, metaDesc, items, locale = "en", link } = A
 </script>
 
 <style>
+  /* Emblème réduit — 54 % des dimensions/tailles de l'emblème d'accueil (clamp(280px,34vw,460px)
+     → clamp(151px,18.4vw,248px) ; 60 % puis encore -10 % relatif, décision Philippe 10/08/2026),
+     même logique de position/repli que HomeSections.astro : décoratif, z-index sous le contenu,
+     masqué sous 720px pour ne jamais chevaucher un titre qui passe sur 2 lignes en mobile.
+     idx__hero doit être positionné pour que l'image s'ancre dans la section, pas dans toute la page. */
+  .idx__hero { position: relative; }
+  .idx__hero > .mdcf-container { position: relative; z-index: 1; }
+  .idx__emblem { position: absolute; z-index: 0; top: 8px;
+    right: max(12px, calc((100% - var(--mdcf-max)) / 2 + 6px));
+    width: clamp(151px, 18.4vw, 248px); height: auto; pointer-events: none; }
+  @media (max-width: 960px) { .idx__emblem { opacity: .16; right: -12px; } }
+  @media (max-width: 720px) { .idx__emblem { display: none; } }
   .idx__blurb { color: var(--mdcf-muted); margin: 0 0 12px; }
   .idx__link { display: inline-flex; align-items: center; gap: 6px; margin: 0 0 16px; padding: 8px 14px;
     border: 1px solid var(--mdcf-line); border-radius: 999px; text-decoration: none; color: var(--mdcf-ink);
diff --git a/packages/ui/src/pages/[...loc]/airlines/index.astro b/packages/ui/src/pages/[...loc]/airlines/index.astro
index 18e5938..6704bd3 100644
--- a/packages/ui/src/pages/[...loc]/airlines/index.astro
+++ b/packages/ui/src/pages/[...loc]/airlines/index.astro
@@ -15,4 +15,4 @@ const T = inlineT(locale);
 const items = indexItems(loadKB(), "airline", locale);
 const seoTitle = T("Airline pet policies: dogs in cabin & hold", "Politiques animaux des compagnies : chien en cabine ou soute", "Políticas de mascotas por aerolínea: perro en cabina o bodega");
 ---
-<EntityIndex seoTitle={seoTitle} kind={t(locale, "kind.airline")} title={t(locale, "index.airlines.title")} blurb={t(locale, "index.airlines.blurb")} items={items} locale={locale} />
+<EntityIndex seoTitle={seoTitle} kind={t(locale, "kind.airline")} title={t(locale, "index.airlines.title")} blurb={t(locale, "index.airlines.blurb")} items={items} locale={locale} showEmblem />
diff --git a/packages/ui/src/pages/[...loc]/breeds/index.astro b/packages/ui/src/pages/[...loc]/breeds/index.astro
index 5742950..a1faf90 100644
--- a/packages/ui/src/pages/[...loc]/breeds/index.astro
+++ b/packages/ui/src/pages/[...loc]/breeds/index.astro
@@ -15,4 +15,4 @@ const T = inlineT(locale);
 const items = indexItems(loadKB(), "breed", locale);
 const seoTitle = T("Can my dog fly? Travel guide breed by breed", "Mon chien peut-il prendre l'avion ? Race par race", "¿Puede volar mi perro? Guía raza por raza");
 ---
-<EntityIndex seoTitle={seoTitle} kind={t(locale, "kind.breed")} title={t(locale, "index.breeds.title")} blurb={t(locale, "index.breeds.blurb")} items={items} locale={locale} />
+<EntityIndex seoTitle={seoTitle} kind={t(locale, "kind.breed")} title={t(locale, "index.breeds.title")} blurb={t(locale, "index.breeds.blurb")} items={items} locale={locale} showEmblem />
diff --git a/packages/ui/src/pages/[...loc]/countries/index.astro b/packages/ui/src/pages/[...loc]/countries/index.astro
index ea78470..c4bc443 100644
--- a/packages/ui/src/pages/[...loc]/countries/index.astro
+++ b/packages/ui/src/pages/[...loc]/countries/index.astro
@@ -15,4 +15,4 @@ const T = inlineT(locale);
 const items = indexItems(loadKB(), "country", locale);
 const seoTitle = T("Dog entry requirements by country (2026)", "Formalités d'entrée par pays pour voyager avec ton chien", "Requisitos de entrada por país para viajar con tu perro");
 ---
-<EntityIndex seoTitle={seoTitle} kind={t(locale, "kind.country")} title={t(locale, "index.countries.title")} blurb={t(locale, "index.countries.blurb")} items={items} locale={locale} />
+<EntityIndex seoTitle={seoTitle} kind={t(locale, "kind.country")} title={t(locale, "index.countries.title")} blurb={t(locale, "index.countries.blurb")} items={items} locale={locale} showEmblem />
diff --git a/packages/ui/src/pages/[...loc]/tools.astro b/packages/ui/src/pages/[...loc]/tools.astro
index c9ba2a3..4e66f85 100644
--- a/packages/ui/src/pages/[...loc]/tools.astro
+++ b/packages/ui/src/pages/[...loc]/tools.astro
@@ -41,7 +41,12 @@ const tools: Tool[] = [
 const seoTitle = T("Free tools for flying with your dog", "Outils gratuits pour voyager en avion avec ton chien", "Herramientas gratuitas para volar con tu perro");
 ---
 <Base locale={locale} title={seoTitle}>
-  <section class="mdcf-section">
+  <section class="mdcf-section idx__hero">
+    {/* Même traitement que EntityIndex.astro (compagnies/destinations/races) : emblème d'accueil
+        repris à 60 % de sa taille, haut droite, alt = titre déjà traduit de la page (demandé pour
+        l'indexation image). Cette page ne passe pas par EntityIndex (liste de liens statiques, pas
+        de recherche/filtre), d'où la duplication du bloc plutôt qu'un composant partagé. */}
+      <img class="idx__emblem" src="/brand/emblem.webp" alt={t(locale, "home.tools.title")} width="440" height="252" decoding="async" loading="lazy" />
     <div class="mdcf-container">
       <p class="mdcf-eyebrow">{t(locale, "nav.tools")}</p>
       <h1 class="mdcf-h2">{t(locale, "home.tools.title")}</h1>
@@ -54,8 +59,19 @@ const seoTitle = T("Free tools for flying with your dog", "Outils gratuits pour
   </section>
 </Base>
 <style>
-  .grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
-  @media(max-width:720px){.grid{grid-template-columns:1fr 1fr}}
+  /* Même formule que EntityIndex.astro — voir son commentaire pour le détail des dimensions. */
+  .idx__hero { position: relative; }
+  .idx__hero > .mdcf-container { position: relative; z-index: 1; }
+  .idx__emblem { position: absolute; z-index: 0; top: 8px;
+    right: max(12px, calc((100% - var(--mdcf-max)) / 2 + 6px));
+    width: clamp(151px, 18.4vw, 248px); height: auto; pointer-events: none; }
+  @media (max-width: 960px) { .idx__emblem { opacity: .16; right: -12px; } }
+  @media (max-width: 720px) { .idx__emblem { display: none; } }
+  /* +100px demandés par Philippe (10/08/2026) pour dégager l'emblème au-dessus de la grille de
+     cartes — seulement là où l'emblème est visible (masqué sous 720px, donc marge remise à zéro
+     à cette largeur pour ne pas laisser un vide inutile en mobile). */
+  .grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:100px}
+  @media(max-width:720px){.grid{grid-template-columns:1fr 1fr;margin-top:0}}
   .tool{display:flex;align-items:center;justify-content:space-between;gap:8px}
   .tool--live{text-decoration:none;color:var(--mdcf-ink);border-color:var(--mdcf-accent);background:var(--mdcf-accent-soft);font-weight:700;transition:transform .15s,box-shadow .15s}
   .tool--live:hover{transform:translateY(-1px);box-shadow:var(--mdcf-shadow)}
