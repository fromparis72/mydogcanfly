here directly: a static tag in this one shared shell would fire on every route, not just "/". Runs before the module script below because classic scripts execute during HTML parsing; type="module" scripts wait for their whole import graph to resolve first. --> Mission , inline, so it applies before the browser has to wait for anything else — no flash of unstyled content between the markup below painting and this styling taking effect. --> Loading site…